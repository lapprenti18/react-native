import React from "react";
import { View } from "react-native";

interface ComponentNameProps {}

const ComponentName = () => {
    return <view style= {{ flex: 1, background: "cyan"}}/>;
};

export default ComponentName;